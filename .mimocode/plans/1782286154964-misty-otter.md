# Plan: Hoàn thiện app học ngoại ngữ cho cá nhân

## Hiện trạng
- Build pass, lint pass, 54 tests pass
- Dual state management: 8 pages dùng `useGlobalState` (old context), 5 pages dùng Zustand stores
- Dual storage: localStorage (old) + Dexie IndexedDB (new)
- Code-split đã có nhưng `index.js` vẫn 625KB (chưa split được vì old context import Dexie)
- Không có component tests, không có gamification thật

---

## Phase 1: Dọn dẹp kiến trúc (nền tảng)

### 1.1 Loại bỏ dual state management
**Mục tiêu**: Tất cả page dùng Zustand + Dexie, xóa `useGlobalState` / `AppStateContext`

| File cần sửa | Thay đổi |
|--------------|----------|
| `src/main.tsx` | Bỏ `AppStateProvider` |
| `src/contexts/AppStateContext.tsx` | Xóa file |
| `src/hooks/useAppState.ts` | Xóa file |
| `src/pages/AddWordsPage.tsx` | `useGlobalState` → `useAppStore` (cards từ Dexie) |
| `src/pages/CollectionPage.tsx` | `useGlobalState` → `useLiveQuery` (Dexie) |
| `src/pages/AnalyticsPage.tsx` | `useGlobalState` → `useLiveQuery` |
| `src/pages/MistakesPage.tsx` | `useGlobalState` → `useLiveQuery` |
| `src/pages/MistakeReviewPage.tsx` | `useGlobalState` → `useLiveQuery` |
| `src/pages/NotebookPage.tsx` | `useGlobalState` → `useLiveQuery` (grammar + kanji từ Dexie) |
| `src/pages/CreateExamPage.tsx` | `useGlobalState` → `useLiveQuery` + `useAppStore` |
| `src/pages/RealExamPage.tsx` | `useGlobalState` → `useAppStore` + `useUserStore` |

### 1.2 Xóa localStorage dependency
- `src/utils/storage.ts`: Giữ lại `exportData`/`importData` nhưng chuyển sang Dexie
- Bỏ các hàm `loadCards`, `saveCards`, `loadMistakes`... (thay bằng Dexie queries)

### 1.3 Giảm bundle size
- Sau khi xóa old context, `index.js` sẽ nhỏ hơn
- Thêm `manualChunks` trong `vite.config.ts` để tách `dexie`, `zustand`, `framer-motion`

---

## Phase 2: Tính năng thiếu cho cá nhân

### 2.1 Spaced Repetition Dashboard
**File mới**: `src/components/ReviewDashboard.tsx`
- Hiển thị: bao nhiêu card due hôm nay, ngày mai, tuần sau
- Breakdown theo difficulty (beginner/intermediate/advanced)
- Trend: cards đã mastered vs đang học vs mới
- Route mới `/review-dashboard`

### 2.2 Mistake Book categorization
**Sửa**: `src/types/index.ts` - thêm `subType` vào `Mistake`
- Phân loại: grammar, vocabulary, listening comprehension, spelling
- Filter trên MistakesPage theo loại

### 2.3 Writing practice scoring improvement
**Sửa**: `src/components/WritingView.tsx`
- Thêm word-by-word highlight chi tiết hơn
- Hiển thị điểm theo từng criteria: vocabulary, grammar, completeness
- Lưu mistake khi score < 80% (đã có, chỉ cần cải thiện UI)

---

## Phase 3: UX & Polish

### 3.1 Xóa console.error không cần thiết
- `src/hooks/useAppState.ts:81` - bỏ console.error (dùng ErrorBoundary)
- `src/data/contentLoader.ts:108` - giữ (quan trọng cho debug DB)
- `src/App.tsx:63` - giữ (quan trọng cho init failure)
- Các file khác giữ nguyên (error handling hợp lý)

### 3.2 Responsive improvements
- Kiểm tra mobile bottom nav không bị overlap nội dung
- Thêm `pb-20` cho main content trên mobile (đã có `pb-40`)

### 3.3 Accessibility
- Thêm `aria-label` cho tất cả interactive elements
- Kiểm tra color contrast cho dark mode
- Thêm keyboard navigation cho quiz/flashcard

---

## Phase 4: Testing

### 4.1 Component tests
- Thêm test cho `FlashcardView`, `QuizView`, `VocabQuizView`
- Thêm test cho `GamifiedPath` rendering logic
- Dùng vitest + jsdom (đã setup)

### 4.2 Integration tests
- Test flow: add card → review → rate → check SM-2 update
- Test flow: start session → complete tasks → check progress unlock

---

## Thứ tự thực hiện

| Ưu tiên | Phase | Thời gian ước tính |
|---------|-------|-------------------|
| 1 | Phase 1.1 - Loại bỏ dual state | 2-3h |
| 2 | Phase 1.2 - Xóa localStorage | 1h |
| 3 | Phase 1.3 - Code split | 30 phút |
| 4 | Phase 2.1 - Review Dashboard | 1-2h |
| 5 | Phase 2.2 - Mistake categorization | 30 phút |
| 6 | Phase 2.3 - Writing scoring | 30 phút |
| 7 | Phase 3.1 - Xóa console | 15 phút |
| 8 | Phase 4.1 - Component tests | 2h |

---

## Verification
1. `npm run build` - pass, không có warning về bundle size
2. `npm run lint` - pass
3. `npm run test` - tất cả pass
4. Manual test: mở app trên browser, test flow học từ, review, quiz
5. Kiểm tra IndexedDB trong DevTools: data được lưu đúng
6. Kiểm tra PWA: offline mode hoạt động
